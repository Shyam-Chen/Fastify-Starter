{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "spec": "vitest --run -c vite.spec-config.ts",
    "meas": "vitest --run -c vite.meas-config.ts"
  },
  "dependencies": {
    "autocannon": "^7.10.0",
    "got": "^12.5.2"
  },
  "devDependencies": {
    "@types/autocannon": "^7.9.0",
    "vitest": "^0.24.5"
  }
}
